# Copilot Instructions — {{ .Config.Metadata.Name }}

> Auto-generated by lzctl from lzctl.yaml. Re-run `lzctl init --force` to regenerate.

## Architecture

- **Connectivity model**: {{ .Config.Spec.Platform.Connectivity.Type }}
- **Management groups**: {{ .Config.Spec.Platform.ManagementGroups.Model }}
- **Primary region**: {{ .Config.Metadata.PrimaryRegion }}
- **CI/CD**: {{ .Config.Spec.CICD.Platform }} ({{ .Config.Spec.CICD.EffectiveModel }} mode)

## Required Tags

All resources must include these tags (defined in `lzctl.yaml`):
- `environment`
- `owner`
- `cost-center`

## Naming Convention

Follow CAF naming: `<resource-type>-<workload>-<env>-<region>-<instance>`
Example: `rg-{{ .Config.Metadata.Name | slugify }}-prod-{{ .Config.Metadata.PrimaryRegion | regionShort }}-001`

## Guardrails — FORBIDDEN Patterns

The following patterns are prohibited and will be rejected by CI policy gates:

- ❌ `public_network_access_enabled = true` on Key Vault, Storage, ACR
- ❌ Hardcoded secrets or connection strings in `.tf` or `.tfvars` files
- ❌ `bypass_policy_lockout_check = true`
- ❌ Resources outside the approved regions ({{ .Config.Metadata.PrimaryRegion }}{{ if .Config.Metadata.SecondaryRegion }}, {{ .Config.Metadata.SecondaryRegion }}{{ end }})
{{ if .Config.Spec.Governance.Policies.EffectivePoliciesEnabled -}}
- ❌ Policy assignments removal or exemption without documented justification
{{ end -}}

## Layers & State

Terraform state is split per layer. Never mix resources across layers.
Layer order: `management-groups → identity → management → governance → connectivity → landing-zones/*`

## Cross-layer References

Use `terraform_remote_state` data sources only. Never hardcode resource IDs.
