# Generated by lzctl {{ .Version }} — safe to edit
terraform {
  required_version = ">= 1.5.0"
}

module "management_groups_lite" {
  source  = "Azure/avm-ptn-alz/azurerm"
  version = "0.11.0"

  architecture_definition_name = "alz"
  location                     = "{{ .Config.Metadata.PrimaryRegion }}"
}
