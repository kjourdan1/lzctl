# Generated by lzctl {{ .Version }} — safe to edit
#
# Shared backend configuration — passed via: terraform init -backend-config=backend.hcl
#
# State Life Management:
#   • Blob lease locking prevents concurrent terraform apply
#   • Blob versioning + soft delete must be enabled on the storage account
#   • Run 'lzctl state health' to verify backend security posture
#   • Run 'lzctl state snapshot --all' before terraform apply
#
resource_group_name  = "{{ .Config.Spec.StateBackend.ResourceGroup }}"
storage_account_name = "{{ .Config.Spec.StateBackend.StorageAccount }}"
container_name       = "{{ .Config.Spec.StateBackend.Container }}"
subscription_id      = "{{ .Config.Spec.StateBackend.Subscription }}"
use_azuread_auth     = true
