# Generated by lzctl {{ .Version }} — safe to edit
terraform {
  required_version = ">= 1.5.0"
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}
  subscription_id = "{{ .Config.Spec.StateBackend.Subscription }}"
  tenant_id       = "{{ .Config.Metadata.Tenant }}"
}
