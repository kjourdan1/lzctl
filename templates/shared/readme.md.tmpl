# Generated by lzctl {{ .Version }} — safe to edit

# {{ .Config.Metadata.Name }}

This repository was generated by `lzctl` and contains the Terraform foundation for your Azure landing zone.

## Quickstart

1. Authenticate:
   - `az login --tenant {{ .Config.Metadata.Tenant }}`
2. Validate:
   - `terraform -chdir=platform/shared init`
   - `terraform -chdir=platform/shared validate`
3. Plan and apply through your CI/CD pipeline.

## Configuration source of truth

- `lzctl.yaml`

## Target regions

- Primary: `{{ .Config.Metadata.PrimaryRegion }}`
{{- if .Config.Metadata.SecondaryRegion }}
- Secondary: `{{ .Config.Metadata.SecondaryRegion }}`
{{- end }}
