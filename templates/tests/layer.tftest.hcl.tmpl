# Generated by lzctl — do not edit manually
# Terraform native tests for layer: {{ .Layer }}
# Docs: https://developer.hashicorp.com/terraform/language/tests
# Requires: Terraform >= 1.6
{{ range .Assertions }}
run "{{ .Name }}" {
  command = plan

  assert {
    condition     = {{ .Condition }}
    error_message = "{{ .ErrorMessage }}"
  }
}
{{ end }}
# Smoke test — validates the module can be planned without errors
run "smoke_plan" {
  command = plan
}
